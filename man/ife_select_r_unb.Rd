% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ife_unbalanced.R
\name{ife_select_r_unb}
\alias{ife_select_r_unb}
\title{Factor Number Selection for Unbalanced Panel IFE via SVT}
\usage{
ife_select_r_unb(formula, data, index, c_f = 0.6, nu_NT = NULL, verbose = TRUE)
}
\arguments{
\item{formula}{R formula: \code{outcome ~ covariate1 + covariate2 + ...}}

\item{data}{Data frame in long format.}

\item{index}{Character vector of length 2: \code{c("unit_col", "time_col")}.}

\item{c_f}{SVT threshold constant (default 0.6).}

\item{nu_NT}{Optional scalar or vector of NNR penalty values. If
\code{NULL} (default), cross-validates over
\code{c(0.01, 0.1, 1, 10) * sqrt(max(N, TT))}.}

\item{verbose}{Logical; print result table. Default \code{TRUE}.}
}
\value{
Invisibly returns a list with components \code{r_hat}, \code{sv}
(normalised singular values), \code{threshold}, \code{c_f},
\code{c_NT}, and \code{nu_used}.
}
\description{
Estimates the number of interactive factors in an unbalanced panel using
the singular value thresholding (SVT) rule of Su, Wang and Wang (2025,
Section 3.3, eq. 3.7).
}
\examples{
\donttest{
  data(cigar, package = "xtife")
  set.seed(42)
  cigar_unb <- cigar[sample(nrow(cigar), 1200L), ]
  ife_select_r_unb(sales ~ price, data = cigar_unb,
                   index = c("state", "year"))
}
}
\references{
Su, L., Wang, X. and Wang, Z. (2025). Estimation and inference for
unbalanced panel data models with interactive fixed effects.
\emph{SSRN Working Paper} 5177283.
}
